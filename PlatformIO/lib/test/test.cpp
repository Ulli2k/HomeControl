
#include <test.h>
// #include <../myRFM69/myRFM69globals.h>

// template <class SPIType, uint8_t iPin>
// void myRadio<SPIType,iPin>::initialize() {
  // spi.init();
  // DS("reset RFM...");
  // resetRFM69();
  // DS("done\n");
  // do spi.writeReg(REG_SYNCVALUE1, 0xAA); while (spi.readReg(REG_SYNCVALUE1, 0x7F) != 0xAA);
  // do spi.writeReg(REG_SYNCVALUE1, 0x55); while (spi.readReg(REG_SYNCVALUE1, 0x7F) != 0x55);
  // DS("RFM cleared\n");
// 	for (byte i = 0; config[i][0] != 255; i++)
// 		spi.writeReg(config[i][0], config[i][1]);

  // pinMode(7, INPUT);           // set pin to input
  // digitalWrite(7, HIGH);
  // cfgInterrupt(this, 7, Interrupt_Rising);
// }


// void myRadio::interrupt() {
//   DS("IRQ");
// }
